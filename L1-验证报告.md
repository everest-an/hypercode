# L1 验证报告:子代理改名(displayName 覆盖机制)

> 验证日期:2026-08-22 · 方式:omo 源码级证据链验证(未跑 TUI 视觉验证,见 §4)
> 结论:**机制确认可用,零源码改动,钩子零风险**。评级:**可行 ✅**

---

## 1. 结论

omo 官方提供 **`displayName` 配置覆盖**——改 TUI 显示名、不改内部代理 ID。钩子/委派路由按内部 ID 匹配,**完全不受影响**。我们只需在配置里写覆盖,连 omo 源码都不用碰(SUL-1.0 合规也更干净)。

## 2. 证据链(源码级)

| 证据 | 位置 | 内容 |
|---|---|---|
| Schema 定义 | `omo-opencode/src/config/schema/agent-overrides.ts:33` | `displayName: z.string().optional()` + 注释 "Localized display name shown in TUI agent selector" |
| 实现 | `src/cli/run/runnable-agent-resolver.ts:32` | `config.agents?.[resolvedConfigKey]?.displayName` — 按**内部名**取显示名 |
| 内部 ID 不变 | `src/cli/run/agent-resolver.ts:23-24` | `isKnownAgent = displayName !== configKey` — configKey 才是身份 |
| 任意字符串支持 | `runnable-agent-resolver.test.ts:59` | 测试用例 displayName 用中文 "总指挥" ✅ |
| 团队模式兼容 | `features/team-mode/*` | displayName 仅用于显示/路由标签,不改变身份解析 |

## 3. 落地配置模板

文件:`hypercode/omo-branding-template.jsonc`(安装器预写进用户 `omo.json`)

```jsonc
{
  "agents": {
    "sisyphus":         { "displayName": "Lead",      "description": "Lead engineer: decomposes and executes development tasks" },
    "sisyphus-junior":  { "displayName": "Worker",    "description": "Worker: completes individual subtasks" },
    "oracle":           { "displayName": "Architect", "description": "Read-only consultant for hard problems" },
    "momus":            { "displayName": "Reviewer",  "description": "Reviews plans and deliverables" },
    "prometheus":       { "displayName": "Planner",   "description": "Exploratory task planning" },
    "metis":            { "displayName": "Analyst",   "description": "Requirement and ambiguity analysis" },
    "librarian":        { "displayName": "Researcher","description": "External documentation and reference research" },
    "explore":          { "displayName": "Scout",     "description": "Codebase exploration and location" },
    "hephaestus":       { "displayName": "Builder",   "description": "Default build and implementation agent" },
    "atlas":            { "displayName": "Mapper",    "description": "Code map and navigation" },
    "multimodal-looker":{ "displayName": "Looker",    "description": "Image and document content analysis" }
  }
}
```

> 注:`sisyphus` 的显示名用 **"Lead"** 而非映射表里的 "HyperCode"——避免"产品名=代理名"在 TUI 里造成的怪异感(打开 HyperCode 看到代理叫 HyperCode)。如不同意可改回。

## 4. 已知边界(如实)

| 项 | 状态 |
|---|---|
| TUI 代理选择器(Tab)显示 displayName | ✅ 文档明确 + 测试覆盖 |
| 子代理会话标题是否用 displayName | ⚠️ 需运行时视觉验证(文档只说 agent selector) |
| 钩子/委派/团队模式路由 | ✅ 按内部 ID,不受影响 |
| `oh-my-openagent` 品牌残留(插件 ID、telemetry 包名、omo.json 文件名) | ⚠️ 另案处理:插件 ID 在 opencode 插件系统里可见,需在引擎层改名或接受;omo.json 可改为产品化路径 |
| 中文显示名 | ✅ 测试已证("总指挥") |

## 5. 运行时视觉验证(待办)

需要可运行的完整安装环境(hypercode 二进制 + omo 插件 + 覆盖配置)后执行:
1. Tab 切换代理 → 确认显示 Lead/Architect/... 
2. 跑一个含委派的真实任务 → 确认子代理正常出场且钩子工作
3. 检查会话列表标题文案

**当前结论不影响推进**:机制已代码级确认,配置模板已就绪,风险仅在"个别界面角落的显示文案",发现后补覆盖即可。
