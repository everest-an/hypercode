# HyperCode 子代理改名方案(omo 去指纹)

> 目标:用户看到的子代理名不暴露 oh-my-openagent 指纹(oracle/momus/prometheus/Sisyphus 等)。
> 已核实机制:omo 提供 `AgentOverrideConfig = Partial<AgentConfig>` 配置层覆盖(name/description/model 等 21 字段),**无需改 omo 源码**。

---

## 1. 调研结果(源码级,2026-08-22)

### 1.1 子代理花名册(omo-opencode Ultimate 版)

| 代理 | 职责 | 名字出现文件数 | 指纹强度 |
|---|---|---|---|
| sisyphus | 主编排者(实现/委派) | 290 | ★★★(最独特) |
| sisyphus-junior | 任务执行者 | — | ★★★ |
| oracle | 只读高智商咨询 | 266 | ★★★ |
| momus | 计划评审 | 229 | ★★★ |
| prometheus | 探索式规划 | 162 | ★★★ |
| metis | 预规划分析 | 159 | ★★★ |
| librarian | 外部引用调研 | 266 | ★★ |
| explore | 代码库探索 | 419(含泛用词) | ★★ |
| hephaestus | 默认 build 增强 | — | ★★★ |
| atlas | 代码地图/导航 | — | ★★ |
| multimodal-looker | 媒体分析 | — | ★(通用) |
| build / plan / general | OpenCode 内建 | — | ★(通用,可保留) |

### 1.2 关键发现

1. **覆盖机制存在**:`AgentOverrideConfig = Partial<AgentConfig> & {...}`——名字、描述、模型、温度等都可从 `hypercode.json` 覆盖,omo 源码零改动。
2. **风险**:omo 内部钩子按代理名字符串匹配(如 `agent === "oracle"`)。**若覆盖 `name` 字段,钩子可能失配**。需实测验证(装 omo 跑一轮钩子回归)。
3. **两套实现**:代理定义在 `packages/omo-opencode/src/agents/`,但运行时还经 `dynamic-prompt-builder` 等组装——覆盖层的最终显示名行为需实测。

---

## 2. 推荐策略:三层防御

| 层 | 做法 | 风险 | 工作量 |
|---|---|---|---|
| **L1 配置覆盖(首选)** | `hypercode.json` 写 `agent: { overrides: { oracle: { name/description... } } }` | 钩子失配(需实测) | 0.5-1 人日 |
| **L2 最小 patch(兜底)** | 若 L1 钩子失配:对 omo 发行物打极小 patch(同步改钩子匹配字符串) | 随升级重打,但 patch 可脚本化 | 1-2 人日 |
| **L3 源码 fork(不推荐)** | 彻底 fork omo 源码改名 | 更新地狱(160-419 文件/名字) | 数周 |

**结论**:先 L1,实测钩子行为;不满足再上 L2;L3 永远不用。

---

## 3. 名字映射提案(待批准)

| 现名 | 提案新名 | 中文含义 | 提案描述文案 |
|---|---|---|---|
| sisyphus | **hypercode** | 主工程师 | "HyperCode 主工程师:负责分解与执行开发任务" |
| sisyphus-junior | **hypercode-worker** | 执行员 | "HyperCode 执行员:专注完成单个子任务" |
| oracle | **architect** | 架构师 | "架构师:只读高难问题咨询" |
| momus | **reviewer** | 评审员 | "评审员:计划与交付物评审" |
| prometheus | **planner** | 规划师 | "规划师:探索式任务规划" |
| metis | **analyst** | 分析师 | "分析师:需求与歧义分析" |
| librarian | **researcher** | 研究员 | "研究员:外部文档与参考调研" |
| explore | **scout** | 侦察员 | "侦察员:代码库探索与定位" |
| hephaestus | **builder** | 建造师 | "建造师:默认构建与实现" |
| atlas | **mapper** | 制图师 | "制图师:代码地图与导航" |
| multimodal-looker | **looker** | 观察员 | "观察员:图像与文档内容分析" |
| build / plan / general | 保留 | — | 通用名,无指纹 |

备选方案(若你更喜欢中文名直接显示):架构师/评审员/规划师/分析师/研究员/侦察员/建造师/制图师/观察员/主工程师/执行员。

---

## 4. awareness → HyperMemory

"awareness" 只出现在机制借鉴层面,我们**自研实现**(本地 SQLite),用户侧品牌统一为 **HyperMemory**:
- 命令:`/remember` `/recall` `/forget`(无 awareness 字样)
- 界面:Omega 面板里叫"记忆"标签
- 文档/帮助:一律 HyperMemory

---

## 5. 执行清单(✅ L1 已代码级确认,2026-08-22)

1. ✅ 已确认:`displayName` 覆盖机制存在且按内部名解析,钩子/路由零影响(详见 `L1-验证报告.md`)
2. ✅ 已产出配置模板:`omo-branding-template.jsonc`(安装器预写)
3. 待办:运行时视觉验证(装完 omo 后跑委派任务,确认会话标题等边角文案)
4. 待办:插件 ID `oh-my-openagent` 品牌残留处理(引擎层改名或接受)
