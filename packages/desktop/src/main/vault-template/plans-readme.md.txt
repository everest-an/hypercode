# Plans

This directory holds Sisyphus/Prometheus planning documents.

> 🔒 Protected: agents are denied Edit/Write, patching, and shell writes on
> `sisyphus/plans/**` at the engine level (enforced by the `vault-guard` plugin and your
> vault permission config). Plans placed here are read by agents for context but can only
> be modified by you.
